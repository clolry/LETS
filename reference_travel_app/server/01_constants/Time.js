/**
 * Time.js
 * Time-unit constants + Eastern timezone string (federal agency standard).
 */

const TIME_CONSTANTS = {
  MS_PER_DAY: 1000 * 60 * 60 * 24,
  MS_PER_HOUR: 1000 * 60 * 60,
  MS_PER_MINUTE: 1000 * 60
};

// Eastern timezone — federal agency standard for all storage timestamps
const EASTERN_TIMEZONE = 'America/New_York';
